extends layout 

block content
  .nav 
    h1 Etchmon - Blog 
  .container 
    if (posts)
      each post in posts 
        .post 
          h1 #{post.title}
          p #{post.date}
          button.delete-post(id=post._id) x
          button o
  script(src='/js/catalog.js')