extends layout 

block content
  .nav
    h1 Etchmon - Blog
  .container 
    form(action="/api/posts" method="POST")
      input(type="text" placeholder="Title" name="title" required="true")
      input(type="textarea" placeholder="..." required="true" name="text")
      button(type="submit") Submit 
    if (errors)
      p #{errors}